- name: Reload systemd daemon
  systemd:
    daemon_reload: true

- name: Restart beacon
  systemd:
    name: beacon.service
    state: restarted

- name: Reload nginx
  systemd:
    name: nginx
    state: reloaded
