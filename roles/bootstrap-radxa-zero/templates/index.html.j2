<!doctype html>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{{ bootstrap_radxa_zero_hostname }}</title>
<style>
  body { font: 16px/1.6 system-ui, sans-serif; margin: 4rem auto; max-width: 34rem; padding: 0 1rem; }
  code { background: #f2f2f2; padding: 0.1em 0.35em; border-radius: 3px; }
</style>
<h1>{{ bootstrap_radxa_zero_hostname }}</h1>
<p>
  nginx is serving <code>{{ bootstrap_radxa_zero_nginx_root }}</code> on port
  {{ bootstrap_radxa_zero_nginx_port }}.
</p>
<p>
  This placeholder is written only while that directory is empty. Drop a site
  in and it will not come back.
</p>
